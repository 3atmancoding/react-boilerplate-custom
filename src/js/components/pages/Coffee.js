import React, { Component } from "react";
import styles from "./stylesheets/Coffee.module.css"
class Coffee extends Component {
   render() {
      return (
         <>
            <h1 className={styles.heading}>Coffee</h1>
         </>
      );
   }
}

export default Coffee;
